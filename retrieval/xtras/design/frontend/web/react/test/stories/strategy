Provide the command to install the latest stable package version of storybook.

Install package command: `insert command here`

If you believe supporting libraries should be used, list up to 3 such libraries and a single install command which installs them all.

Install supporting libraries command: `insert command here`
Provide the command to install the latest stable package version of each frontend Unit testing library.

Install package command: `insert command here`

If you believe multiple libraries should be used, list up to 3 such libraries and a single install command which installs them all.
